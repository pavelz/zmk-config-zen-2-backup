on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: pavelz/zmk/.github/workflows/build-user-config.yml@corne-ish-zen-v2
    steps:
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
